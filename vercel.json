{
  "version": 2,
  "buildCommand": "npm install --prefix ./frontend && npm run build --prefix ./frontend",
  "outputDirectory": "frontend/dist",
  "routes": [
    {
      "src": "/index.html",
      "status": 200
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
